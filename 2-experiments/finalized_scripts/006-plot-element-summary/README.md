To run the script `plot-element.py`, need also the h5 files generated from previous extracting steps.
In the folder `data_final`, h5 files with all data extract from the complete runs can be found, the script can be run from there.

